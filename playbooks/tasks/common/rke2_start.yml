---
- name: Gather RKE2 service facts before start
  ansible.builtin.import_tasks: rke2_service_facts.yml

- name: Start RKE2 service
  ansible.builtin.service:
    name: "{{ rke2_service_name }}"
    state: started
